import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  standalone: true,
  template: `
    <main class="container py-5">
      <h1>Game Catalog</h1>
      <p class="text-muted">Application is bootstrapping...</p>
    </main>
  `
})
export class AppComponent {}
